# Experiments

This directory stores quick prototypes and exploratory scripts that are not full projects.

- `scratch/`: ad-hoc experiments.

If an experiment grows into a real project, move it into `projects/<project_name>/`.
